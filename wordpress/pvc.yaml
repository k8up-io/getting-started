apiVersion: v1
kind: PersistentVolumeClaim
metadata:
  name: wordpress-pvc
  annotations:
    appuio.ch/backup: "true"
  labels:
    app: wordpress
spec:
  accessModes:
    - ReadWriteOnce
  resources:
    requests:
      storage: 10Gi
